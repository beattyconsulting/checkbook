import React, { Component } from 'react'
const base = 'register'

class App extends Component {

  render () {
    return (
      <div className={`${base}`}>Hello</div>
    )
  }
}

export default App