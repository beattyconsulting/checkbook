import {Component} from "react"

const base = 'register'

export class Register extends Component {
    render() {
        return (
            <div className={`${base}`}>
                TBD Checking Register
            </div>
        )
    }
}